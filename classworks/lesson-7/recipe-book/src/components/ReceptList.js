import React from 'react';
import { connect } from 'react-redux';
import { bindActionsCreators } from 'redux';

const ReceptList = props => {
  return <div></div>;
};

export default connect()(ReceptList);
